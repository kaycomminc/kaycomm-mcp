{
  "name": "kaycomm-mcp",
  "version": "2.0.0",
  "description": "KayComm Google Ads + Meta + GA4 MCP Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "node-cron": "^4.6.0",
    "nodemailer": "^9.0.4",
    "pg": "^8.23.0"
  }
}
